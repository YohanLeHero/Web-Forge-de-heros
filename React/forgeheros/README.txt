React + Vite. Calls Symfony via /api (proxied to http://127.0.0.1:8000). Start backend first.

  npm install
  npm run dev

  npm run build   → dist/

Backend: ../../Symfony/Forge-des-heros (composer run serve — see README.txt there).

Need: Node.js + npm.
